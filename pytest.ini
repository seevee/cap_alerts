# Required by pytest-homeassistant-custom-component: its `hass` fixture is
# async and needs pytest-asyncio auto mode. Explicit @pytest.mark.asyncio
# marks elsewhere in the suite are unaffected.
[pytest]
asyncio_mode = auto
markers =
    validate_scope: run the real config-flow scope validation instead of the stub
