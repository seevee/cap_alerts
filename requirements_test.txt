# Test, lint, and type-check dependencies. Referenced by .github/workflows/ci.yml
# and by local tooling (see AGENTS.md "Build & Test Commands").

homeassistant==2026.9.3
# Pins its own homeassistant version — keep in lockstep with the pin above
# (0.13.366 pins homeassistant==2026.9.3).
pytest-homeassistant-custom-component==0.13.366

pytest>=8.0,<10
pytest-asyncio>=1.0,<2
# Pinned exactly: CI gates on the reported percentage, and coverage's own
# measurement can shift between minor releases. Configuration is in .coveragerc.
pytest-cov==7.1.0
# Property-based tests over the parsers (tests/test_property_*.py). Pinned
# exactly: the example database and shrinking behave differently across
# versions, and a failing example should reproduce the same way in CI.
hypothesis==6.168.0
aiohttp>=3.10,<4
# The one runtime requirement; the same pin as manifest.json so tests run
# what Home Assistant installs.
defusedxml==0.7.1

ruff==0.16.8
mypy==2.3.1
types-defusedxml>=0.7

# Changelog generation (ships a prebuilt `git-cliff` binary as a wheel).
# Regenerate CHANGELOG.md with: git cliff --config cliff.toml --output CHANGELOG.md
git-cliff==2.14.2
